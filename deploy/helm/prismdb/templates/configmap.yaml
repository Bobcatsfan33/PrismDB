apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "prismdb.fullname" . }}-topology
  labels:
    {{- include "prismdb.labels" . | nindent 4 }}
data:
  topology.json: |
    {{- .Values.topology | toJson | nindent 4 }}
